import styled from 'styled-components'

export const ResumeTitleWrapper = styled.li`
  font-size: 1.2em;
  padding: 0.6em 1em;
  border-left: 3px solid #0078d4;
  background-color: var(--nav-light);
  color: var(--font-black);
`